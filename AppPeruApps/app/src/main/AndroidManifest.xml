<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appperuapps">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controlador.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controlador.CrearTareas" />
        <activity android:name=".controlador.TareasFinalizadas" />
        <activity android:name=".controlador.GruposActivity" />
        <activity android:name=".controlador.CrearGrupoTareas"></activity>
        <receiver android:name=".controlador.AlarmReceiver" android:exported="true" android:enabled="true"> </receiver>
        <receiver android:name=".controlador.GestionarNotificacion" android:exported="true" android:enabled="true"> </receiver>
        <activity android:theme="@android:style/Theme.Dialog" android:name=".controlador.GestionarAlerta"> </activity>
    </application>

</manifest>